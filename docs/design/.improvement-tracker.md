# Design Doc Improvement Tracker

## State

phase: planner
iteration: 48
status: active

## Current Suggestion

<!-- Filled by Planner. Cleared after each iteration. -->

## Decision

<!-- Filled by Approver. Cleared after each iteration. -->

## Denied Suggestions

<!-- Compact list for quick Planner reference. Format: id: summary (reason) -->
37: Section 3 terminal output example shows unsorted active links (expected_changes incomplete — piped output example has same unsorted order and was not included in the fix)

## History

<!-- Append-only log. Format below. -->
<!--
### Iteration N
- category: <consistency|gap|clarity|logic|workflow|stdlib>
- summary: <one-line>
- files: <affected files>
- verdict: accepted | denied
- changes_made: <brief description> (if accepted)
- reason: <denial reason> (if denied)
-->

### Iteration 42
- category: logic
- summary: Broken Link Handling sets IsBroken:true for all filepath.EvalSymlinks failures, but status.md and stdlib.md define broken strictly as os.IsNotExist, so a permission-denied symlink is misclassified as broken and may be incorrectly pruned.
- files: internals.md
- verdict: accepted
- changes_made: Replaced step 5 in Broken Link Handling with a two-branch check: call os.Stat(resolvedTarget) first; only set IsBroken: true if os.IsNotExist(err); for any other error (e.g., permission denied), skip the link as unclassifiable.

### Iteration 1
- category: gap
- summary: The execute phase references `originalMode` without specifying when or how to capture the file mode before the move.
- files: orphan.md
- verdict: accepted
- changes_made: Inserted step 2 ("Read original file mode from `link.Target` via `os.Lstat`: store `info.Mode()` for use in step 5") into the Execute Mode sequence; renumbered subsequent steps (3–6); updated rollback reference from "step (2 or 3)" to "step (3 or 4)".

### Iteration 2
- category: consistency
- summary: Status output examples omit the `✓` icon on the Total line, contradicting output.md's specification that every command uses PrintSummary (which calls PrintSuccess and thus adds `✓`) for its summary step.
- files: status.md, output.md
- verdict: accepted
- changes_made: Updated all three Total line examples in status.md (sections 3 and 8) to include the `✓` icon prefix, consistent with output.md's PrintSummary spec and every other command's summary format.

### Iteration 3
- category: consistency
- summary: The dry-run output flow in output.md has an extra "blank line" step after PrintCommandHeader that would produce a double blank line, contradicting all command-specific dry-run examples.
- files: output.md
- verdict: accepted
- changes_made: Removed the extra "blank line" step 2 from the dry-run flow in Section 6 and renumbered the remaining steps (3→2, 4→3, 5→4), so the flow now reads: 1. PrintCommandHeader, 2. PrintDryRun, 3. blank line, 4. PrintDryRunSummary.

### Iteration 4
- category: gap
- summary: The post-execution summary logic lists independent `if created > 0` and `if failed > 0` conditions but provides no output example for the partial-success case, leaving it ambiguous whether PrintNextStep is still called and what the message order is.
- files: create.md, remove.md, prune.md
- verdict: accepted
- changes_made: Split the next-step hint out of the `created/removed/pruned > 0` bullet into a separate condition (`only when failed == 0`) in all three files; added a partial-success output example to Section 8 (create.md) and Section 7 (remove.md, prune.md).

### Iteration 5
- category: gap
- summary: Phase 1 step 3 says "apply steps 4–7" for directory-collected files, omitting step 8 (ValidateSymlinkCreation), so circular-reference and overlapping-path checks are skipped for directory arguments.
- files: adopt.md
- verdict: accepted
- changes_made: Changed "apply steps 4–7" to "apply steps 4–8" in Phase 1 step 3 so that ValidateSymlinkCreation is applied to all files collected from directory arguments, consistent with single-file argument handling.

### Iteration 6
- category: consistency
- summary: adopt.md's MoveFile Behavior description (Section 5) omits the file mode preservation steps that internals.md specifies, so an implementer reading adopt.md would produce a cross-device copy that silently discards file permissions.
- files: adopt.md, internals.md
- verdict: accepted
- changes_made: Expanded the cross-device fallback bullet list in Section 5 to include the two missing steps: read `src` file mode via `os.Lstat` and apply the original file mode to `dst` via `os.Chmod`, matching internals.md's specification.

### Iteration 7
- category: gap
- summary: LoadIgnoreFile specifies behavior when the file does not exist but leaves the read-failure case (file exists but is unreadable) unspecified, so implementers must guess whether to return an error or silently continue.
- files: internals.md
- verdict: accepted
- changes_made: Split step 3 ("Reads the file and parses it line by line") into two steps: step 3 covers opening/reading with explicit error propagation (`nil, err` on I/O failure), and step 4 covers parsing; former step 4 renumbered to step 5.

### Iteration 8
- category: gap
- summary: adopt.md's rollback section specifies the rollback steps but not what to return when a rollback step itself fails, leaving implementers to guess — unlike orphan.md which explicitly specifies a combined error for the same scenario.
- files: adopt.md
- verdict: accepted
- changes_made: Added a third indented bullet under the rollback step list in Phase 2 specifying that a rollback-step failure returns a combined error (e.g., `"adopt failed: <err>; rollback failed: <err>"`), matching orphan.md's specification.

### Iteration 9
- category: gap
- summary: The broken link handling fallback in FindManagedLinks uses a singular `source` variable without specifying iteration over all `sources`, unlike the normal path which explicitly says "for any source in `sources`".
- files: internals.md
- verdict: accepted
- changes_made: Replaced steps 4–5 in the Broken Link Handling subsection with explicit multi-source iteration language matching the main Behavior section; added "using the matching source as the `Source` field" to step 5.

### Iteration 10
- category: consistency
- summary: adopt.md Phase 1 Step 8 calls `ValidateSymlinkCreation(absPath, destPath)` but the arguments are reversed — adopt creates a symlink AT `absPath` pointing TO `destPath`, so `source=destPath` and `target=absPath` per the function's documented semantics.
- files: adopt.md
- verdict: accepted
- changes_made: Corrected argument order from `ValidateSymlinkCreation(absPath, destPath)` to `ValidateSymlinkCreation(destPath, absPath)` in Phase 1 Step 8; added inline comment clarifying source/target roles.

### Iteration 11
- category: consistency
- summary: adopt.md's dry-run example omits the blank line and "No changes made in dry-run mode" output line, inconsistent with every other command spec (create.md, remove.md, prune.md, orphan.md) which all show the complete dry-run output.
- files: adopt.md
- verdict: accepted
- changes_made: Replaced the code block + trailing prose sentence ("End with `PrintDryRunSummary()`.") with a single complete code block that includes the blank line separator and "No changes made in dry-run mode" footer, matching the format used by all other command specs.

### Iteration 12
- category: gap
- summary: The managed link detection code in Section 4 ignores the error from `filepath.EvalSymlinks`, allowing a broken symlink at the expected target path to be misclassified as managed and incorrectly removed.
- files: remove.md
- verdict: accepted
- changes_made: Added `if err != nil { continue }` guard after `filepath.EvalSymlinks` call in the Section 4 code block; appended a sentence after "Links that do not meet this criterion are ignored silently." explaining that `EvalSymlinks` errors cause the path to be skipped silently.

### Iteration 13
- category: logic
- summary: Startup sequence step 4 says "bare `lnk` with no command" exits 0, directly contradicting Command Dispatch's specification that "no command given (without --version/--help)" exits 2.
- files: cli.md
- verdict: accepted
- changes_made: Changed step 4 wording from "bare `lnk` with no command" to "bare `lnk` (invoked with no arguments at all)", making it unambiguous that step 4 covers only zero-argument invocations; any `lnk [flags]` invocation without a command falls through to Command Dispatch which exits 2.

### Iteration 14
- category: gap
- summary: orphan.md Phase 1 step 5 says "Resolve to absolute path" without specifying the resolution method, so an implementer would likely use `filepath.Abs(rawTarget)` which resolves relative to CWD rather than relative to the symlink's location.
- files: orphan.md
- verdict: accepted
- changes_made: Replaced "Resolve to absolute path" with explicit instructions: if `rawTarget` is relative, use `filepath.Join(filepath.Dir(absPath), rawTarget)` then `filepath.Clean`; if already absolute, use directly. Matches the pattern in internals.md Section 3.

### Iteration 15
- category: gap
- summary: adopt.md Phase 1 step 3 does not specify that only regular files are collected during a directory walk, leaving ambiguous whether symlinks within the directory are adopted.
- files: adopt.md
- verdict: accepted
- changes_made: Changed "walk it and collect each file within" to "walk it recursively and collect each regular file within (`d.Type().IsRegular()`); symlinks and other non-regular entries are skipped" in Phase 1 step 3.

### Iteration 16
- category: gap
- summary: create.md Phase 1 step 1 says "Skip directories" without specifying that symlinks are also skipped, leaving ambiguous whether source-directory symlinks-to-files are collected for linking.
- files: create.md
- verdict: accepted
- changes_made: Changed "Skip directories (only files are linked)" to "Skip non-regular entries: only collect files where `d.Type().IsRegular()`; directories, symlinks, and other special entries are skipped" in Phase 1 step 1.

### Iteration 17
- category: gap
- summary: validateAdoptSource step 3 says "Resolves to an absolute path" without specifying the resolution method, so an implementer would use `filepath.Abs(rawTarget)` which resolves relative to CWD rather than relative to the symlink's location.
- files: internals.md
- verdict: accepted
- changes_made: Replaced "Resolves to an absolute path" in Section 10 step 3 with explicit symlink-relative resolution instructions matching Section 3's Broken Link Handling pattern and orphan.md iteration 14.

### Iteration 18
- category: gap
- summary: MoveFile's cross-device fallback does not specify that the partially-written destination file must be removed when copy or verification fails, leaving an implementer to guess and potentially leaving orphaned partial files on disk.
- files: internals.md, adopt.md
- verdict: accepted
- changes_made: Added "If copy or verification fails: call `os.Remove(dst)` (ignore any removal error) before returning the error" bullet after the size-verification step in both internals.md Section 6 and adopt.md Section 5.

### Iteration 19
- category: gap
- summary: Phase 1 step 4 (directory argument) adds all FindManagedLinks results to the collection without filtering broken links, letting them silently pass Phase 1 and fail in Phase 2, violating the transactional guarantee and contradicting the error table that classifies "Broken symlink" as a Phase 1 error.
- files: orphan.md
- verdict: accepted
- changes_made: Added broken-link filter loop after the "none found" check in Phase 1 step 4: for each link where `IsBroken == true`, return a `PathError` with hint to use `rm` directly; changed "Add all found links" to "Add only active links to the collection".

### Iteration 20
- category: consistency
- summary: Phase 3 Execute Mode step 2 says "regular file" for the CreateSymlink collision case, but Section 6 (Collision Handling) and internals.md Section 4 both say "regular file or directory", leaving an implementer reading Phase 3 to miss the directory-at-target collision.
- files: create.md
- verdict: accepted
- changes_made: Changed "If target is a regular file: return error with hint to use `adopt`" to "If target is a regular file or directory: return error with hint to use `adopt`" in Phase 3 Execute Mode step 2.

### Iteration 21
- category: consistency
- summary: remove.md Section 4's managed-link containment check omits the `rel != "."` guard, so a symlink pointing to the sourceDir itself is misclassified as managed and incorrectly removed.
- files: remove.md, internals.md
- verdict: accepted
- changes_made: Added `&& rel != "."` to the `isManaged` check in Section 4 code block, matching the guard specified in internals.md Section 3.

### Iteration 22
- category: clarity
- summary: The `ManagedLink.Target` field comment says "raw symlink target value (as stored on disk)" but FindManagedLinks populates it with the absolute resolved path, so an implementer following the comment would store the raw `os.Readlink` result and break orphan Phase 2 operations that treat `link.Target` as an absolute path.
- files: internals.md
- verdict: accepted
- changes_made: Changed `Target` field comment from "raw symlink target value (as stored on disk)" to "absolute path of the symlink's resolved target (never relative)"; added explicit Target assignment in FindManagedLinks Behavior step 4; updated Broken Link Handling step 5 to name both Target and Source field assignments.

### Iteration 23
- category: consistency
- summary: status.md's ManagedLink.Target field comment still says "raw symlink target value", contradicting the corrected "absolute path of the symlink's resolved target (never relative)" that iteration 22 applied to internals.md.
- files: status.md
- verdict: accepted
- changes_made: Changed `Target` field comment from "raw symlink target value" to "absolute path of the symlink's resolved target (never relative)" in status.md Section 3 Step 1, matching internals.md post-iteration-22.

### Iteration 24
- category: consistency
- summary: Phase 1 step 5's containment check omits the `rel != "."` guard that Section 4 explicitly requires, so a symlink pointing directly at sourceDir would be misclassified as managed and could trigger MoveFile on the entire source directory.
- files: orphan.md
- verdict: accepted
- changes_made: Replaced "Verify target is within `absSourceDir` via `filepath.Rel`: if not, return `LinkError` with hint to use `rm` directly" with explicit `rel, _ := filepath.Rel(absSourceDir, resolvedTarget)` computation and dual guard checking both `..` prefix and `"."` equality before returning `LinkError`.

### Iteration 25
- category: logic
- summary: Execute Mode rollback trigger "If any step (3 or 4) fails" misses step 1 failure, leaving previously-completed orphans stranded in a changed state and violating the atomicity guarantee.
- files: orphan.md
- verdict: accepted
- changes_made: Changed "If any step (3 or 4) fails:" to "If any step (1, 3, or 4) fails:" in Phase 2 Execute Mode rollback trigger.

### Iteration 26
- category: clarity
- summary: The Execute Mode rollback says "all completed orphans" but this excludes the failing orphan itself, so an implementer would skip restoring the symlink when step 4 fails after step 3 succeeded.
- files: orphan.md
- verdict: accepted
- changes_made: Changed "Roll back all completed orphans in reverse order:" to "Roll back in reverse order all orphans up to and including the failing one (the per-step conditionals handle partial state):" in Phase 2 Execute Mode rollback block.

### Iteration 27
- category: consistency
- summary: The containment check pattern in stdlib.md Section 5 omits the `rel != "."` guard, inconsistent with every operation spec that performs the same check.
- files: stdlib.md
- verdict: accepted
- changes_made: Added `&& rel != "."` to the `isWithin` expression in Section 5's containment check code block, matching the guard used in internals.md, remove.md, and orphan.md.

### Iteration 28
- category: consistency
- summary: adopt.md's Phase 2 rollback still says "all completed adoptions" and lacks the "(if moved)" guard, so the failing adoption's partial state (file moved but no symlink) is never rolled back — the same flaw iteration 26 corrected in orphan.md.
- files: adopt.md
- verdict: accepted
- changes_made: Changed "Roll back all completed adoptions in reverse order:" to "Roll back in reverse order all adoptions up to and including the failing one (the per-step conditionals handle partial state):" and added "(if moved)" guard to the MoveFile rollback step.

### Iteration 29
- category: gap
- summary: MoveFile's cross-device fallback does not specify what to do when `os.Chmod` fails, leaving an implementer to guess whether it is fatal (abort the copy) or non-fatal (warn and continue).
- files: internals.md, adopt.md
- verdict: accepted
- changes_made: Extended the `os.Chmod` bullet in both internals.md Section 6 and adopt.md Section 5 to specify that `os.Chmod` failure logs a warning via `PrintVerbose` and continues — permission restoration is best-effort and does not abort the copy.

### Iteration 31
- category: consistency
- summary: FindManagedLinks Behavior step 4 does not name the `Source` field when creating a ManagedLink, inconsistent with Broken Link Handling step 5 which explicitly sets it.
- files: internals.md
- verdict: accepted
- changes_made: Added ", and `Source` to the matching source directory" to the "If matched" bullet in Behavior step 4, making it consistent with Broken Link Handling step 5's explicit field naming.

### Iteration 32
- category: logic
- summary: FindManagedLinks steps 2 and 3 are ordered so that the macOS Library/.Trash skip (step 3) is unreachable — step 2's non-symlink check returns nil for directory entries before step 3 can fire.
- files: internals.md
- verdict: accepted
- changes_made: Swapped steps 2 and 3 in FindManagedLinks Behavior so the macOS Library/.Trash SkipDir check (new step 2) precedes the non-symlink filter (new step 3), ensuring the macOS skip fires for directory entries before they are returned nil by the non-symlink check.

### Iteration 33
- category: consistency
- summary: The dry-run output examples in create.md and adopt.md omit the command header line, inconsistent with remove.md, prune.md, and orphan.md which all show it.
- files: create.md, adopt.md
- verdict: accepted
- changes_made: Added "Creating Symlinks\n\n" header prefix to create.md Phase 3 Dry-Run Mode code block; added "Adopting Files\n\n" header prefix to adopt.md Section 3 Dry-Run Mode code block, matching the dry-run format used by all other command specs.

### Iteration 34
- category: clarity
- summary: The next-step hint condition "only when `failed == 0`" conflicts with the "all already exist" output example, which shows no hint — two implementers would disagree.
- files: create.md
- verdict: accepted
- changes_made: Changed "Print next-step hint only when `failed == 0`" to "Print next-step hint only when `created > 0` and `failed == 0`" in Phase 3 Execute Mode, making the condition consistent with the Section 8 "All links already exist" output example.

### Iteration 35
- category: consistency
- summary: PrintCommandHeader's code always prints a blank line even in piped mode, but status.md's piped output example shows no leading blank line, leaving two implementers to disagree.
- files: output.md, status.md
- verdict: accepted
- changes_made: Updated the terminal/piped table entry for PrintCommandHeader from "blank line only" to "nothing (no output)"; moved fmt.Println() inside the if !ShouldSimplifyOutput() block so the blank line is terminal-only, matching the status.md piped output example.

### Iteration 36
- category: gap
- summary: PrintNextStep does not specify whether sourceDir is contracted via ContractPath before display, leaving implementers to show absolute paths instead of ~/... paths in next-step hints.
- files: output.md
- verdict: accepted
- changes_made: Replaced the single-sentence PrintNextStep definition with three sentences specifying that the function applies ContractPath to sourceDir before formatting the output string, and that callers pass the expanded absolute path.

### Iteration 37
- category: consistency
- summary: Section 3's terminal output example shows active links in non-alphabetical order, contradicting Step 2's lexicographic sort and Section 8's correctly-sorted examples.
- files: status.md
- verdict: denied
- reason: expected_changes incomplete — the piped output example in Section 3 has the same unsorted order and was not included in the fix; accepting would create a new inconsistency.

### Iteration 38
- category: logic
- summary: The Execute Mode rollback trigger lists only steps (1, 3, or 4), omitting step 2, so an os.Lstat failure leaves previously completed orphans permanently changed — violating the atomicity guarantee.
- files: orphan.md
- verdict: accepted
- changes_made: Changed "If any step (1, 3, or 4) fails:" to "If any step (1, 2, 3, or 4) fails:" in Phase 2 Execute Mode rollback trigger.

### Iteration 39
- category: consistency
- summary: Section 3 terminal and piped output examples both show active links in wrong lexicographic order, contradicting Step 2's sort requirement and Section 8's correctly-sorted examples.
- files: status.md
- verdict: accepted
- changes_made: Reordered active links in Section 3 terminal output example (~/.vimrc → ~/.config/git/config → ~/.vimrc to ~/.bashrc → ~/.config/git/config → ~/.vimrc) and piped output example (same reordering), matching Step 2's lexicographic ascending sort requirement and Section 8's correct ordering.

### Iteration 40
- category: gap
- summary: Phase 1 has no validation to reject symlinks pointing outside sourceDir, silently adopting them despite the stated NonGoal of "Adopting symlinks that already point elsewhere."
- files: adopt.md, error-handling.md
- verdict: accepted
- changes_made: Added second bullet to Phase 1 step 4 rejecting non-adopted symlinks with PathError and hint to remove first; added "Path is a non-adopted symlink" row to Section 9 error table; added matching row to error-handling.md Section 11 Adopt table.

### Iteration 30
- category: gap
- summary: The piped output section in Step 3 does not specify link ordering, leaving it ambiguous whether links print in global path-sort order (interleaving active/broken) or in the grouped active-first-then-broken order shown by terminal output.
- files: status.md
- verdict: accepted
- changes_made: Replaced the piped output intro sentence with explicit active-before-broken ordering language: "active links are printed first (in path-sorted order), then broken links (in path-sorted order) — the same active-before-broken grouping as terminal output, but without icons or blank-line separators."

### Iteration 41
- category: gap
- summary: output.md has no functions that output `✗ <message>` to stdout or `active`/`broken` prefixed lines in piped mode, leaving implementers unable to produce the status-command link display without inventing their own functions or misusing PrintError (which writes to stderr).
- files: output.md
- verdict: accepted
- changes_made: Added `PrintActiveLink` and `PrintBrokenLink` rows to the Terminal vs. Piped Formats table (after `PrintSuccess`) and to the Verbosity Gating table (both shown in Normal and Verbose modes). Both functions write to stdout.

### Iteration 43
- category: gap
- summary: output.md lacks a PrintWarningWithHint function, leaving implementers unable to produce the per-item warning-with-hint format shown in create.md's partial success example.
- files: output.md
- verdict: accepted
- changes_made: Added `PrintWarningWithHint(err error)` block to Section 5 Specialized Functions, after `PrintErrorWithHint`, specifying terminal (`! <err>` + `  Try: <hint>`) and piped (`warning: <err>` + `hint: <hint>`) formats, both on stderr.

### Iteration 44
- category: consistency
- summary: orphan.md Phase 2 Execute step 5 says "log it and continue" for chmod failure without naming the output function, inconsistent with adopt.md which explicitly says "log a warning via PrintVerbose and continue" for the same scenario.
- files: orphan.md
- verdict: accepted
- changes_made: Changed "Failure here is a warning only; log it and continue" to "Failure here is a warning only; log it via `PrintVerbose` and continue — permission restoration is best-effort and does not abort the orphan" in Phase 2 Execute Mode step 5.

### Iteration 45
- category: clarity
- summary: The partial success output example in Section 8 shows "permission denied" paired with the adopt hint, but Section 6 says the adopt hint is only for regular file/directory collisions — two implementers would disagree on whether the hint applies to all failures or only file collisions.
- files: create.md
- verdict: accepted
- changes_made: Changed error message in Section 8 partial success output example from "permission denied" to "regular file exists at target", making it consistent with Section 6 which scopes the adopt hint exclusively to regular file/directory collisions.

### Iteration 46
- category: gap
- summary: Phase 3 Execute Mode step 5 says "print warning" without specifying `PrintWarningWithHint`, so an implementer would call `PrintWarning(err.Error())` and silently drop the actionable hint shown in the Section 8 partial success example.
- files: create.md
- verdict: accepted
- changes_made: Changed "On failure: print warning and increment failure counter; continue with remaining links" to "On failure: call `PrintWarningWithHint(err)` and increment failure counter; continue with remaining links" in Phase 3 Execute Mode step 5.

### Iteration 47
- category: consistency
- summary: Execute mode step 3 in remove.md and prune.md says "print error" for per-item failures, but create.md step 5 explicitly says `PrintWarningWithHint(err)` — an implementer could use `PrintError` (showing `✗ Error:`) instead of the correct `!`-prefixed warning format.
- files: remove.md, prune.md
- verdict: accepted
- changes_made: Changed "On failure: print error and increment failure counter; continue with remaining links" to "On failure: call `PrintWarningWithHint(err)` and increment failure counter; continue with remaining links" in step 3 of Execute Mode in both remove.md and prune.md.
